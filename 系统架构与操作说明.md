# QMini 机器人系统架构与操作说明

## 一、系统整体架构

### 1.1 系统概述
这是一个用于控制 QMini 机器人的软件系统，支持在 PC、Jetson Nano、Raspberry Pi 等平台上运行。系统采用 C++ 和 Python 混合编程，使用 DDS（Data Distribution Service）进行实时通信，集成强化学习（RL）控制器进行运动控制。

### 1.2 核心组件

#### 1.2.1 主要类结构
- **G1 类** (`include/user/custom.hpp`): 系统主控制器，负责整体协调
- **RLController 类** (`include/user/rl_controller.h`): 强化学习控制器
- **ModeSwitcher 类** (`include/user/mode_switcher.h`): 模式切换器
- **JoystickReader 类** (`include/user/joystick_reader.h`): 手柄输入读取器
- **IMUReader 类** (`include/user/IMUReader.h`): IMU 数据读取器
- **MotorController 类** (`include/user/Motor_thread.hpp`): 电机控制器

#### 1.2.2 多线程架构
系统采用多线程设计，各线程及其运行频率如下：

| 线程名称 | 功能 | 运行频率 | 说明 |
|---------|------|---------|------|
| `control_thread` | 主控制循环 | 0.015s (66.7Hz) | 执行 RL 控制算法，处理运动指令 |
| `command_writer` | 电机指令发送 | 0.002s (500Hz) | 将控制指令发送到电机 |
| `imu_thread` | IMU 数据读取 | 0.003s (333Hz) | 读取 IMU 传感器数据 |
| `joystick_thread` | 手柄数据读取 | 0.004s (250Hz) | 读取手柄输入 |
| `mode_process` | 模式切换处理 | 0.02s (50Hz) | 处理模式切换逻辑 |
| `report_rpy` | 数据报告 | 0.015s (66.7Hz) | 报告机器人状态数据 |

### 1.3 数据流架构

```
手柄输入 (joystick.py)
    ↓
JoystickReader (C++ 通过 Python API 调用)
    ↓
ModeSwitcher (模式识别)
    ↓
RLController (控制算法)
    ↓
MotorCommand (电机指令)
    ↓
DDS 通信层
    ↓
电机执行器

IMU 传感器
    ↓
IMUReader
    ↓
BaseState (机器人状态)
    ↓
RLController (状态反馈)
```

### 1.4 配置文件

**config.yaml** 包含以下关键参数：
- `control_dt`: 控制周期 (0.015s)
- `vx_cmd_range`: X 方向速度命令范围 [-0.3, 0.5] m/s
- `yr_cmd_range`: Yaw 角速度命令范围 [-1, 1] rad/s
- `kp`, `kd`: 位置和速度控制增益
- `act_pos_low/high`: 关节位置限制
- `ref_joint_act`: 参考关节位置（站立姿态零位）

### 1.5 机器人零位记录

系统中有多个与零位相关的关节位置记录：

#### 1.5.1 参考关节位置 (ref_joint_act)
- **定义位置**: `config.yaml` 中的 `ref_joint_act` 参数
- **当前值**: `[0.745, -0.030, -0.672, 1.206, 1.070, -0.416, 0.712, 1.537, -0.298, 1.359]`
- **含义**: 机器人站立姿态的参考关节位置（零位）
- **用途**: 
  - 作为站立控制的目标位置
  - 作为关节位置增量的参考基准
  - 在 `stand_control()` 函数中使用

#### 1.5.2 初始关节位置 (init_joint_act)
- **定义位置**: `RLController::reset()` 函数中设置
- **设置时机**: 每次模式切换或系统重置时
- **设置方式**:
  - 测试模式 (`is_test_local=true`): 使用当前 `joint_act` 值
  - 实际运行模式: 从实际关节位置 `joint_pos` 读取
- **用途**: 
  - 作为平滑过渡的起始位置
  - 在 `smooth_joint_action()` 中用于从当前位置平滑过渡到目标位置

#### 1.5.3 关节位置说明
系统共有 **10 个关节**，按顺序对应：
1. 左腿髋关节 Yaw (C_l_hyaw)
2. 左腿髋关节 Roll (C_l_hrol)
3. 左腿髋关节 Pitch (C_l_hpit)
4. 左腿膝关节 (C_l_knee)
5. 左腿踝关节 Pitch (C_l_apit)
6. 右腿髋关节 Yaw (C_r_hyaw)
7. 右腿髋关节 Roll (C_r_hrol)
8. 右腿髋关节 Pitch (C_r_hpit)
9. 右腿膝关节 (C_r_knee)
10. 右腿踝关节 Pitch (C_r_apit)

#### 1.5.4 零位使用流程

```
系统启动
    ↓
加载 config.yaml 中的 ref_joint_act
    ↓
初始化时: joint_act = _ref_joint_act
    ↓
模式切换时调用 reset():
    - 读取当前关节位置 joint_pos
    - 设置 init_joint_act = joint_pos (当前实际位置)
    ↓
stand_control() 执行:
    - 从 init_joint_act 平滑过渡到 _ref_joint_act
    - 使用插值: joint_act = (1-ratio) * init_joint_act + ratio * _ref_joint_act
```

#### 1.5.5 零位修改方法

如需修改机器人零位（站立姿态），需要：
1. 修改 `config.yaml` 中的 `ref_joint_act` 参数
2. 确保新值在 `act_pos_low` 和 `act_pos_high` 限制范围内
3. 重新编译并运行程序

**注意**: 修改零位前请确保了解各关节的物理限制，避免损坏机器人。

## 二、操作模式说明

### 2.1 模式列表

系统支持以下操作模式：

| 模式代码 | 模式名称 | 手柄按键 | 功能说明 |
|---------|---------|---------|---------|
| '1' | Folding (折叠) | START | 机器人折叠状态，准备就绪 |
| '2' | Standing (站立) | A | 机器人站立模式 |
| '3' | RL Walking (强化学习行走) | Y | 使用 RL 模型进行行走控制 |
| '4' | RL Stand (RL 站立) | X | 使用 RL 模型进行站立控制 |
| '5' | Sin Test (正弦测试) | SELECT | 正弦波测试模式 |
| '6' | Lateral (横向移动) | L1 | 横向移动模式 |
| '7' | - | R1 | 预留模式 7 |
| '8' | - | L2 | 预留模式 8 |
| '9' | - | R2 | 预留模式 9 |
| 'q' | Exit (退出) | B | 退出程序 |

### 2.2 模式切换逻辑

- 模式切换有安全限制，不能随意切换
- 从模式 '1' 只能切换到模式 '2'
- 从模式 '2' 可以切换到模式 '3' 或其他模式
- 模式切换时会重置控制器状态

## 三、手柄键位映射

### 3.1 手柄按键定义

手柄通过 `joystick.py` (Python) 读取，然后通过 `JoystickReader` (C++) 解析。

#### 3.1.1 摇杆 (Axis)
- **LaxiX** (Axis[0]): 左摇杆 X 轴 (-1.0 ~ 1.0)
- **LaxiY** (Axis[1]): 左摇杆 Y 轴 (-1.0 ~ 1.0) - **用于控制前进/后退速度**
- **RaxiX** (Axis[2]): 右摇杆 X 轴 (-1.0 ~ 1.0) - **用于控制转向角速度**
- **RaxiY** (Axis[3]): 右摇杆 Y 轴 (-1.0 ~ 1.0)

#### 3.1.2 方向键 (Hat)
- **hatX**: 方向键 X 轴 (-1, 0, 1)
- **hatY**: 方向键 Y 轴 (-1, 0, 1)

#### 3.1.3 功能按键 (Buttons)
- **butA** (But[0]): A 键 - **模式 2 (站立)**
- **butB** (But[1]): B 键 - **退出程序**
- **butX** (But[2]): X 键 - **模式 4 (RL 站立)**
- **butY** (But[3]): Y 键 - **模式 3 (RL 行走)**
- **L1** (But[4]): L1 键 - **模式 6 (横向移动)**
- **R1** (But[5]): R1 键 - **模式 7**
- **L2** (But[6]): L2 键 - **模式 8**
- **R2** (But[7]): R2 键 - **模式 9**
- **SELECT** (But[8]): SELECT 键 - **模式 5 (正弦测试)**
- **START** (But[9]): START 键 - **模式 1 (准备就绪)**

### 3.2 运动控制指令

在 **模式 3 (RL Walking)** 和 **模式 4 (RL Stand)** 下，摇杆控制如下：

#### 3.2.1 速度控制
- **左摇杆 Y 轴 (LaxiY)**: 控制前进/后退速度
  - 向前推: 前进，最大速度 0.5 m/s
  - 向后拉: 后退，最大速度 0.3 m/s
  - 计算公式: `vx_cmd = -vx_max * LaxiY`
  
#### 3.2.2 转向控制
- **右摇杆 X 轴 (RaxiX)**: 控制转向角速度
  - 向右推: 右转，最大角速度 1.0 rad/s
  - 向左推: 左转，最大角速度 -1.0 rad/s
  - 计算公式: `yr_cmd = -yr_max * RaxiX`

#### 3.2.3 姿态保持
- 当转向角速度小于 0.1 rad/s 时，系统会自动保持当前航向角
- 使用 PID 控制保持姿态: `yr_cmd = kp_yaw_ctrl * angle_error`

## 四、系统运行流程

### 4.1 初始化流程

1. **系统初始化** (`G1::G1()`)
   - 初始化 DDS 通信通道
   - 初始化手柄读取器 (Python)
   - 初始化 RL 控制器
   - 加载 ONNX 模型 (`policy.onnx`)
   - 读取配置文件 (`config.yaml`)
   - 创建所有工作线程

2. **线程启动顺序**
   - Control 线程 (控制循环)
   - Command Writer 线程 (电机指令)
   - IMU 线程 (传感器数据)
   - Joystick 线程 (手柄输入)
   - Mode Process 线程 (模式切换)
   - Report 线程 (数据报告)

### 4.2 主控制循环

```
Control 线程 (15ms 周期):
  1. 读取当前机器人状态 (关节位置、速度、IMU 数据)
  2. 根据当前模式执行相应控制算法:
     - 模式 '1': 折叠状态 (按键: **START**)
     - 模式 '2': 站立控制 (按键: **A**)
     - 模式 '3': RL 行走控制 (按键: **Y**)
     - 模式 '4': RL 站立控制 (按键: **X**)
     - 模式 '5': 正弦测试 (按键: **SELECT**)
  3. 处理手柄输入，生成速度命令
  4. 通过 ONNX 模型推理生成关节动作
  5. 设置电机目标位置和增益
  6. 将指令写入数据缓冲区
```

### 4.3 电机控制流程

```
Command Writer 线程 (2ms 周期):
  1. 从数据缓冲区读取电机指令
  2. 转换为 DDS 消息格式
  3. 通过 DDS 发布到电机执行器
  4. 读取电机反馈状态
  5. 更新状态缓冲区
```

## 五、强化学习控制器

### 5.1 RL 模型
- 模型文件: `policy.onnx`
- 输入维度: 43 (观测空间)
- 输出维度: 12 (动作空间)
- 观测堆叠: 3 帧历史数据

### 5.2 观测空间 (43 维)
包含以下信息:
- 关节位置 (10 维)
- 关节速度 (10 维)
- 关节目标位置 (10 维)
- 关节目标速度 (10 维)
- 基座姿态 (Roll, Pitch, Yaw) (3 维)
- 基座角速度 (3 维)
- 基座加速度 (3 维)
- 基座四元数 (4 维)
- 速度命令 (vx, yr) (2 维)
- 相位信息 (4 维)

### 5.3 动作空间 (12 维)
- 关节位置增量 (10 维)
- 相位调制参数 (2 维)

## 六、关键文件说明

### 6.1 源代码文件
- `src/run_interface.cpp`: 程序入口
- `src/user/custom.cpp`: G1 类实现
- `src/user/rl_controller.cpp`: RL 控制器实现
- `include/user/custom.hpp`: G1 类定义
- `include/user/rl_controller.h`: RL 控制器定义

### 6.2 配置文件
- `config.yaml`: 系统配置参数
- `policy.onnx`: 强化学习模型文件

### 6.3 Python 脚本
- `joystick.py`: 手柄输入读取 (使用 pygame)

### 6.4 数据文件
- `general.txt`: 通用数据记录
- `rl.txt`: RL 训练数据记录

## 七、使用说明

### 7.1 启动系统

**x86_64 平台:**
```bash
export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:install/x86_64/lib/
./install/x86_64/bin/run_interface
```

**aarch64 平台 (Jetson):**
```bash
export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:install/aarch64/lib/
./install/aarch64/bin/run_interface
```

### 7.2 操作步骤

1. **准备阶段**: 按下 **START** 键，进入模式 1 (准备就绪)
2. **站立**: 按下 **A** 键，进入模式 2 (站立)
3. **行走**: 按下 **Y** 键，进入模式 3 (RL 行走)
   - 使用左摇杆 Y 轴控制前进/后退
   - 使用右摇杆 X 轴控制转向
4. **退出**: 按下 **B** 键，退出程序

### 7.3 注意事项

- 模式切换需要按顺序进行，不能跳跃切换
- 在 RL 模式下，摇杆输入会实时影响机器人运动
- 系统会自动记录运行数据到 `general.txt` 和 `rl.txt`
- 确保手柄正确连接并被系统识别

## 八、技术栈

- **编程语言**: C++, Python
- **通信协议**: DDS (Data Distribution Service)
- **机器学习**: ONNX Runtime
- **数学库**: Eigen3
- **配置文件**: YAML (yaml-cpp)
- **JSON 处理**: jsoncpp
- **手柄输入**: pygame (Python)
- **机器人 SDK**: Unitree SDK2

---

**文档版本**: 1.0  
**最后更新**: 2024

